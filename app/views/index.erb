<h1>All Tasks</h1>

<% @tasks.each do |task| %>
        <i class='glyphicon glyphicon-briefcase'></i>
  <h3><%= task.id %>: <a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
<% end %>

<!-- gives title to bullet points within tasks  -->
